#ifndef DRAWOBJECTS_H
#define DRAWOBJECTS_H
#include "Malla.hpp"
#include "Curva.h"

void drawObjects(bool animado, bool relleno, Spline curva, Malla *malla); // solo dibuja los ejes si esta sin animar

#endif
